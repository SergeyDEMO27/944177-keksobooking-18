'use strict';
(function () {
  var townMap = document.querySelector('.map');
  var mapForm = document.querySelector('.map__filters');

  window.util = {
    ESC_KEYCODE: 27,
    townMap: townMap,
    mapForm: mapForm
  };
})();
